import { noop } from './maverick-gr-NKUum.js';
import { effect as effect$1 } from './maverick-Dgy9432B.js';

/**
 * Invokes the given function each time any of the signals that are read inside are updated
 * (i.e., their value changes). The effect is immediately invoked on initialization.
 *
 * @see {@link https://github.com/maverick-js/signals#effect}
 */
const effect = (serverEffect ) ;

function serverEffect(effect, options) {
  if (typeof process !== 'undefined' && process.env?.NODE_ENV === 'test') {
    return effect$1(effect);
  }

  return noop;
}

export { effect };
